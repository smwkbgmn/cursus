#ifndef OCF_HPP
# define OCF_HPP

# include <iostream>

class OCF
{
	public:
	/*
		ENUM
		STRUCT - FORWARD DECLARE
		TYPEDEF
	*/

	/* ATTRIBUTE */
	/* INSTANTIATE */
		OCF( void );
		OCF( const OCF & );
		~OCF( void );

	/* METHOD */
	/* OPERATOR */
		OCF	&operator=( const OCF & );

	/* STRUCT - DEFINE */
};

#endif