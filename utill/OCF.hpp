#ifndef OCF_HPP
# define OCF_HPP

# include <iostream>

class OCF
{
	/* TYPEDEF */
	/* ATTRIBUTE */
	/* INSTANTIATE */
	public:
		OCF( void );
		OCF( const OCF & );
		~OCF( void );

	/* METHOD */
	
	/* OPERATOR */
	public:
		OCF	&operator=( const OCF & );

	/* STRUCT */
};

#endif